from setuptools import setup

setup(name='fractal',
      version='0.1',
      description='Foo',
      url='https://github.com/jjge732/fractal-machine',
      author='Bar',
      packages=['project'],
      zip_safe=False)
